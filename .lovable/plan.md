

## План: убрать angle-фото с потёртостью + добавить автовращение

### Проблема
Фото `black_bra_3d_angle.png` — старое, с потёртостями/артефактами. Его нужно удалить. Также нужно медленное автовращение.

### Шаги

1. **Удалить `src/assets/black_bra_3d_angle.png`** — это фото с потёртостью.

2. **Обновить `products.ts`** — товар ID 10:
   - Убрать импорт `braAngle`
   - `spinImages: [braFront, braBack]` — только 2 актуальных фото

3. **Обновить `ProductImageSpin.tsx`**:
   - Поддержать 2 фото: front → front-mirrored → back → back-mirrored (4 кадра)
   - Добавить **автовращение**: `setInterval` каждые ~2.5 секунды переключает кадр
   - Автовращение **останавливается** при drag и **возобновляется** через 3 сек после отпускания

### Результат
Топ медленно крутится сам, без потёртых фото. Пользователь может перехватить drag-ом.

