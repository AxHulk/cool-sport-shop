RewriteEngine On
RewriteBase /

# Если запрашиваемый файл или директория существует, отдаем их
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Иначе перенаправляем все запросы на index.html
RewriteRule ^ index.html [L]
